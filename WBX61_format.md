# WBX61 CSV 数据格式说明

版本：v1.0  
日期：2026-09-04

## 1. 文件结构

WBX61 CSV 是**无表头的混合记录流**。每行第 1 个字段是记录类型码；不同类型的字段数和含义不同。

解析要求：

1. 使用 CSV 解析器读取，同时兼容 `CRLF` 和 `LF` 行尾。
2. 第 1 个字段先按字符串处理，因为存在 `APP_MSG` 等非数字类型。
3. 先按类型码分流，再校验该类型的字段数和数值字段。
4. 未知类型、未知字段和原始行号必须保留，不得静默丢弃。
5. 未出现的通道表示“本文件无该观测”，不得补零。

## 2. 时间

### 2.1 文件采集起点

文件名包含唯一的 17 位采集起点：

```text
YYYYMMDDHHMMSSmmm
```

其中 `mmm` 为毫秒。例如：

```text
心率-A01_日常_静坐-家燕--20260826173446638-D368D6AA80D8.csv
                          └─ 2026-08-26 17:34:46.638
```

WBX61 文件名中的采集起点固定使用北京时间，即 `Asia/Shanghai`（UTC+08:00）。解析器必须按该时区解析，不能使用 UTC 或运行环境时区代替。

解析器必须验证文件名中恰有一个合法的 17 位时间串。若文件被重命名而缺少该时间，必须由外部提供采集起点，不得自动改用其他字段替代。

### 2.2 行时间

`t_rel` 是该行相对于文件采集起点的时间，单位为毫秒：

```text
t_abs = t0_from_filename + t_rel_ms
```

每种数据类型拥有独立时间流。合并不同类型时必须按 `t_rel` 或 `t_abs` 对齐，不能按 CSV 行号配对。

`t_rel` 和 `epoch` 均应使用至少 64 位整数保存。

`1002/1004` 中的 `epoch` 是 Polar 数据包到达手机 APP 的 Unix epoch 毫秒时间。Unix epoch 数值本身不含时区；转换为日历时间、显示或与文件名时间对齐时，统一使用北京时间 `Asia/Shanghai`（UTC+08:00）。`epoch` 不是心搏发生时间，也不能覆盖由文件名和 `t_rel` 得到的行绝对时间；两种时间应分别保存。

## 3. 原始传感器类型

### 3.1 PPG

PPG 行格式：

```text
<type>,<raw_value>,<t_rel_ms>
```

WBX61 有 3 种光源，每种光源对应 4 个 PD（Photodiode，光电二极管），共 12 路光学信号：

| 光源 | PD1 | PD2 | PD3 | PD4 |
|---|---:|---:|---:|---:|
| 绿光 | `1` | `9` | `10` | `11` |
| 红光 | `5` | `12` | `13` | `14` |
| 红外 | `4` | `15` | `16` | `17` |

4 路 PD 分别记录为 4 种类型，不是同一行的 4 个数值；多通道分析只对齐 `t_rel` 相同的记录。

`raw_value` 是 PPG 原始计数。ADC 位宽、增益、满量程和物理单位不在该行中，未经设备协议确认不得换算为电压或光强。

不同采集模式可以只输出部分光源或部分通道。解析器应按实际出现的类型码建立通道集合。

类型 `8` 表示可选的 ECG I 导联记录；当前样例未出现，行结构和单位尚未确认，遇到时应先原样保留。

### 3.2 ACC

ACC 行格式：

```text
2,<acc_x_raw>,<acc_y_raw>,<acc_z_raw>,<t_rel_ms>
```

必须保留三轴原始值。ACC 缩放不能仅凭设备名称固定，需结合本文件校验。

本项目提供的常见候选换算为：

```text
acc_candidate_mg = acc_raw / 4096.0f
```

该结果只有通过设备协议或物理量级校验后，才能正式标记为 `mg`。

如果设备协议中的 `4096` 实际表示 `4096 counts/g`，则 `/4096` 的结果单位是 `g`，转换为 `mg` 应为：

```text
acc_mg = acc_raw * 1000.0 / 4096.0
       = acc_raw / 4.096
```

WBX61 原始 ACC 数据保留直流重力分量。可选择已确认的静止佩戴区间，计算

```text
M_raw = median(sqrt(x_raw^2 + y_raw^2 + z_raw^2))
M_mg  = M_raw * scale_to_mg
```

`M_mg` 应与约 `1000 mg` 的静态重力模长相符；允许误差应由设备规格或项目验收条件给出。若没有可信静止段，或候选换算不符合物理量级，单位标记为“未确认”，不得修改原始数据或自动选择看似合理的系数。

## 4. Polar H10 数据

### 4.1 心率 `1002`

```text
1002,<hr_bpm>,<t_rel_ms>,<packet_epoch_ms>
```

- `hr_bpm`：Polar H10 心率，单位 `bpm`。
- `packet_epoch_ms`：Polar 数据包到达手机 APP 的 Unix epoch 毫秒时间。
- 这是数据包更新流；应报告实际更新率，不假定严格为 `1 Hz`。

### 4.2 RR 间期 `1004`

```text
1004,<rr_ms>,<t_rel_ms>,<packet_epoch_ms>
```

- `rr_ms`：Polar H10 RR 间期，单位毫秒。
- `packet_epoch_ms`：携带该 RR 的 Polar 数据包到达手机 APP 的时间。
- 一个数据包可以携带多条 RR，因此多行可以共享同一个 `packet_epoch_ms` 或 `t_rel_ms`。
- RR 是按心搏产生的事件序列，没有固定采样率。必须保留文件内顺序，不得按包时间去重，也不得把包到达时间当作 R 峰时间。

## 5. 算法、调试和设备记录

以下结构已在样例中观察到；未确认字段使用中性名称：

| 类型 | 行格式 | 处理规则 |
|---|---|---|
| `101` | `101,<field_2>,<field_3>,<field_4>,<t_rel_ms>,<field_6>` | 算法结果；除 `t_rel_ms` 外的正式含义未确认，原样保留 |
| `222` | `222,0,<message>` | SDK版本、SQI或调试文本；不作为原始传感器样本 |
| `224` | `224,NLM_DEVICE_INFO,<device>,<field_4>,<field_5>,<field_6>,<field_7>,<field_8>` | 设备信息；未知字段按字符串保存，避免丢失 `0001` 等前导零 |
| `112/113/100/225` | 配置记录 | 字段协议未确认，原样保留；不得仅凭其数值确定采样率 |
| `APP_MSG` | `APP_MSG,TotalTime,<value>` | APP汇总记录；单位和取整规则按采集协议确认 |

SQI（Signal Quality Index，信号质量指数）是 SDK 内部质量输出。其数值不能替代外部参考、运动伪影检查或算法准确率评价。

## 6. 采样率推断

采样率必须按**每一种周期采样类型**分别推断，不能只读取文件名，也不能按自然秒统计点数。

对单个类型按原始顺序取得 `t_rel_ms`。时间倒退或复位处先切分连续段；在每段内用唯一时间点计算：

```text
dt_i       = t_unique[i] - t_unique[i-1]
dt_typical = 正向 dt_i 的众数；存在轻微抖动时使用中位数
fs_interval_hz = 1000.0 / dt_typical
fs_span_hz     = 1000.0 * (N_unique - 1) / (t_last_unique - t_first_unique)
```

要求：

1. 同时间同载荷标记为重复记录；同时间不同载荷标记为时间戳冲突。两者均保留，但从采样率估计中排除重复时间点。
2. 时间倒退或复位处必须切分连续段。长间隔应报告其相对典型间隔的倍数，不得静默补点。
3. `fs_interval_hz` 与较长连续段的 `fs_span_hz` 应相互验证；不一致时标记“采样率未确认”。
4. `dt≈40/20/8 ms` 分别支持约 `25/50/125 Hz`，但应同时报告计算值，不强行改成整数标称值。
5. 同一光源的 4 路 PD 应分别推断采样率，并检查时间数组是否一致。
6. 标称约 `25 Hz` 不表示每个自然秒必须恰有 25 点；秒边界、时钟偏差和时间量化都会影响单秒计数。
7. 解析阶段不补点、不删点、不重采样。

`1002` 应分别报告 `t_rel` 更新率和 `packet_epoch_ms` 到达节奏；`1004` 无固定采样率；`101` 应报告算法输出更新率。无 `t_rel` 的配置行和调试行不计算采样率。

## 7. 完整性检查

每个文件至少输出：

- 文件名、SHA-256、采集起点和时区；
- 全部类型码、行数和字段数；
- 每种周期数据的首末 `t_rel`、典型间隔、跨度采样率；
- 重复时间、时间倒退、复位、长间断和缺少的通道；
- PPG零值、长常量段、极值和突变；
- ACC三轴及向量模的原始范围、采用的缩放系数和校验依据；
- Polar心率/RR数量、值域、包时间重复情况和原始顺序；
- 未知类型、畸形行及其源行号。

建议解析后按记录类型分别保存为逻辑表，例如 `raw_ppg`、`raw_acc`、`polar_hr`、`polar_rr`、`algorithm_results` 和 `device_messages`；不要求物理上拆成多个文件。每条记录保留 `source_line`、类型码和完整原始字段；仅对原记录包含 `t_rel` 的类型保存 `t_rel_ms`，并在文件采集起点和采集时区均已确定时计算 `t_abs`。
